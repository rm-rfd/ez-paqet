[English](/README.md) | [فارسی](/README.fa_IR.md)

> [!IMPORTANT]
> این پروژه فقط برای استفاده شخصی است، لطفاً از آن برای مقاصد غیرقانونی استفاده نکنید و لطفاً از آن در محیط تولید استفاده نکنید.

# Ez-Paqet 🚀

Ez-Paqet مجموعه‌ای از اسکریپت‌های نصب خودکار برای **Paqet** است، یک ابزار تونل‌زنی شبکه با کارایی بالا که برای کار حتی در محیط‌های شبکه محدود طراحی شده است. این اسکریپت‌ها فرآیند راه‌اندازی را در سرورها و کلاینت‌های لینوکس ساده می‌کنند و وابستگی‌ها، پیکربندی و سرویس‌های سیستم را به صورت خودکار مدیریت می‌کنند.

## 📖 Paqet چیست؟

Paqet (تلفظ می‌شود "پاکت") ابزاری است که به شما امکان می‌دهد یک "تونل" امن و رمزگذاری شده بین دو کامپیوتر از طریق اینترنت ایجاد کنید. این ابزار به ویژه برای دور زدن محدودیت‌های شبکه، سانسور یا فایروال‌ها با مسیریابی ترافیک از طریق یک سرور راه دور مفید است:

- **سرور (Exit Node):** یک VPS در یک شبکه "آزاد" یا بدون محدودیت.
- **کلاینت (Bridge Node):** یک VPS در یک شبکه محدود که به سرور متصل می‌شود.

پس از اتصال، کلاینت یک پروکسی محلی SOCKS5 ایجاد می‌کند. هر ترافیکی که به این پروکسی ارسال شود، به سرور و سپس به اینترنت تونل می‌شود و محدودیت‌های محلی را دور می‌زند.

---

## 🛠️ پیش‌نیازها

برای برقراری یک تونل کاربردی، به **دو سرور لینوکس مجزا** نیاز دارید:

1. **سرور راه دور (شبکه آزاد):** یک VPS واقع در یک منطقه بدون محدودیت (مانند آمریکا، اروپا).
2. **سرور محلی/پل (شبکه محدود):** یک VPS واقع در منطقه محدود که می‌خواهید فیلترینگ آن را دور بزنید.

هر سرور باید دارای شرایط زیر باشد:

- **دسترسی Root:** امتیازات مدیریتی (توانایی استفاده از `sudo`).
- **سیستم‌عامل‌های پشتیبانی شده:** Ubuntu، Debian، CentOS یا Fedora.
- **دسترسی به اینترنت:** برای دانلود اسکریپت‌های نصب و فایل‌های باینری.

---

## 🚀 راه‌اندازی سریع (توصیه شده)

ساده‌ترین راه برای شروع استفاده از **نصب‌کننده یکپارچه (Unified Installer)** است. این اسکریپت از شما می‌پرسد که آیا می‌خواهید سرور یا کلاینت را راه‌اندازی کنید و بقیه کارها را برای شما انجام می‌دهد.

این دستور را در ترمینال لینوکس خود اجرا کنید:

```bash
sudo bash -c "$(curl -fsSL https://raw.githubusercontent.com/rm-rfd/ez-paqet/main/install.sh)"
```

_اگر `curl` ندارید، می‌توانید به جای آن از `wget` استفاده کنید:_

```bash
sudo bash -c "$(wget -qO- https://raw.githubusercontent.com/rm-rfd/ez-paqet/main/install.sh)"
```

---

## 🖥️ نصب دستی

اگر ترجیح می‌دهید یک جزء خاص را مستقیماً نصب کنید، می‌توانید از اسکریپت‌های جداگانه استفاده کنید.

### ۱. راه‌اندازی سرور

سرور "نقطه خروج" تونل شماست. این را روی یک VPS یا ماشینی با اتصال اینترنت بدون محدودیت نصب کنید.

```bash
sudo bash -c "$(curl -fsSL https://raw.githubusercontent.com/rm-rfd/ez-paqet/main/install-server.sh)"
```

- **چه کاری انجام می‌دهد:** باینری Paqet را نصب می‌کند، یک کلید رمزگذاری منحصر به فرد ایجاد می‌کند و سرویس را شروع می‌کند.
- **مهم:** **IP سرور**، **پورت** و **کلید رمزگذاری** نشان داده شده در پایان را یادداشت کنید. برای کلاینت به این‌ها نیاز خواهید داشت.

### ۲. راه‌اندازی کلاینت

کلاینت "نقطه ورود" است. این را روی دستگاهی که نیاز به دور زدن محدودیت‌ها دارد نصب کنید.

```bash
sudo bash -c "$(curl -fsSL https://raw.githubusercontent.com/rm-rfd/ez-paqet/main/install-client.sh)"
```

- **چه کاری انجام می‌دهد:** وابستگی‌ها را نصب می‌کند، IP سرور و کلید را می‌پرسد و یک پروکسی SOCKS5 راه‌اندازی می‌کند.
  > **نکته درباره پروکسی:** اگر شبکه شما به شدت محدود شده است و حتی نمی‌توانید باینری Paqet یا وابستگی‌های آن را دانلود کنید، اسکریپت از شما می‌خواهد یک پروکسی HTTP/HTTPS وارد کنید. این به نصب‌کننده اجازه می‌دهد تا با استفاده از یک پروکسی موقت که می‌دانید کار می‌کند، از محدودیت‌های اولیه عبور کند.
- **نتیجه:** یک پروکسی محلی در `127.0.0.1:1080` ایجاد می‌کند.

---

## 🔍 نحوه استفاده از تونل

پس از نصب و اجرای کلاینت:

1. **تنظیمات مرورگر:** مرورگر خود (مانند کروم یا فایرفاکس) را برای استفاده از یک **پروکسی SOCKS5** با آدرس `127.0.0.1` و پورت `1080` پیکربندی کنید.
2. **تست ترمینال:** می‌توانید با اجرای دستور زیر از کارکرد آن اطمینان حاصل کنید:
   ```bash
   curl -v https://httpbin.org/ip --proxy socks5h://127.0.0.1:1080
   ```
   در صورت موفقیت آمیز بودن، آدرس IP بازگردانده شده باید همان **IP سرور** شما باشد.

---

## 🛡️ پیشرفته: ادغام با 3x-ui (پنل Xray)

می‌توانید از Paqet به عنوان یک "پل" برای پنل Xray خود (مانند [3x-ui](https://github.com/MHSanaei/3x-ui)) استفاده کنید. این به شما امکان می‌دهد ترافیک VLESS/VMess خود را از طریق تونل Paqet به یک VPS در منطقه اینترنت آزاد هدایت کنید.

### ۱. از اجرای Paqet مطمئن شوید

اطمینان حاصل کنید که کلاینت Paqet شما روی VPS محدود شما در حال اجرا است و به پورت SOCKS5 گوش می‌دهد (پیش‌فرض: `127.0.0.1:1080`).

### ۲. اگر قبلاً پنل را روی VPS محدود خود نصب نکرده‌اید، آن را نصب کنید.

### ۳. پیکربندی Outbound در 3x-ui

باید یک outbound از نوع "Socks" در پنل خود اضافه کنید تا به پورت محلی Paqet متصل شوید.

1. **پنل 3x-ui** خود را باز کنید.
2. به **تنظیمات (Settings)** (یا تنظیمات پنل) بروید.
3. به دنبال **Xray Configuration** (یا Config Template) بگردید.
4. بخش `"outbounds": [...]` را پیدا کنید.
5. شیء زیر را به **ابتدای** لیست outbounds اضافه کنید (تا به مسیر پیش‌فرض تبدیل شود):

```json
{
  "protocol": "socks",
  "settings": {
    "servers": [
      {
        "address": "127.0.0.1",
        "port": 1080,
        "users": []
      }
    ]
  },
  "tag": "paqet-proxy"
}
```

> **توجه:** اگر قبلاً یک outbound از نوع `freedom` (مستقیم) دارید، مطمئن شوید که این بلوک جدید SOCKS **بالای** آن قرار گیرد تا تمام ترافیک به طور خودکار از طریق تونل خصوصی عبور کند.

### ۴. تایید مسیریابی

پس از ذخیره تنظیمات و راه اندازی مجدد سرویس Xray از طریق پنل:

1. **مسیر ورودی (Inbound Path):** کاربر با استفاده از VLESS/VMess به VPS محدود شما متصل می‌شود.
2. **پردازش Xray:** Xray ترافیک را دریافت می‌کند.
3. **مسیر خروجی (Outbound Path):** Xray آن را به **Outbound پیش‌فرض** -> `127.0.0.1:1080` (Paqet) فوروارد می‌کند.
4. **تونل‌زنی:** Paqet ترافیک را بسته‌بندی کرده و به **سرور راه دور** شما در منطقه آزاد می‌فرستد.

---

## ⚙️ مدیریت سرویس

اسکریپت‌ها Paqet را به عنوان یک سرویس پس‌زمینه راه‌اندازی می‌کنند تا با روشن شدن کامپیوتر به طور خودکار شروع به کار کند.

| اقدام               | دستور (کلاینت)                   | دستور (سرور)              |
| :------------------ | :------------------------------- | :------------------------ |
| **بررسی وضعیت**     | `systemctl status paqet-client`  | `systemctl status paqet`  |
| **مشاهده لاگ‌ها**   | `journalctl -u paqet-client -f`  | `journalctl -u paqet -f`  |
| **راه‌اندازی مجدد** | `systemctl restart paqet-client` | `systemctl restart paqet` |
| **توقف**            | `systemctl stop paqet-client`    | `systemctl stop paqet`    |

---

## ⚠️ نکات مهم امنیتی

- **کلید خود را مخفی نگه دارید:** هر کسی که IP سرور، پورت و کلید شما را داشته باشد می‌تواند از تونل شما استفاده کند.
- **فایروال:** مطمئن شوید پورتی که انتخاب کرده‌اید (پیش‌فرض `54321`) در فایروال سرور شما (مانند UFW یا AWS Security Groups) باز است.
- **به‌روزرسانی‌ها:** این اسکریپت‌ها از نسخه خاصی از Paqet استفاده می‌کنند. برای بروزرسانی، ممکن است نیاز باشد دوباره نصب‌کننده را اجرا کنید یا نسخه‌های جدید اسکریپت‌ها را بررسی کنید.

---

## 📄 لایسنس

این پروژه برای مقاصد آموزشی است. لطفاً اطمینان حاصل کنید که استفاده شما از ابزارهای تونل‌زنی با قوانین محلی و شرایط خدمات مطابقت دارد.
